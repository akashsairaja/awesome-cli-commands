{
  "id": "aws-cw-6",
  "categoryId": "aws",
  "subcategoryId": "cloudwatch",
  "title": "Start Insights query",
  "command": "aws logs start-query --log-group-name /aws/lambda/myfunction --start-time $(date -u -d \"1 hour ago\" +%s) --end-time $(date -u +%s) --query-string \"fields @timestamp, @message | filter @type = \\\"ERROR\\\"\" --query QueryId --output text",
  "description": "Start CloudWatch Logs Insights query."
}
