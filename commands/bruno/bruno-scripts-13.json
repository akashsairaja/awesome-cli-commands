{
  "id": "bruno-scripts-13",
  "categoryId": "bruno",
  "subcategoryId": "bruno-scripts",
  "title": "Auth token chaining",
  "command": "script:post-response {\n  if (res.status === 200) {\n    bru.setVar('authToken', res.body.access_token);\n    bru.setVar('refreshToken', res.body.refresh_token);\n  }\n}",
  "description": "Extract and chain auth tokens between requests."
}
