{
  "id": "cypress-fixtures-3",
  "categoryId": "cypress",
  "subcategoryId": "cypress-fixtures",
  "title": "Use aliased fixture",
  "command": "cy.get('@usersData').then((users) => { cy.log(users.name); });",
  "description": "Access aliased fixture data."
}
