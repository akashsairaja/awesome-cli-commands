{
  "id": "docker-100",
  "categoryId": "docker",
  "subcategoryId": "buildx",
  "title": "Build and push multi-platform",
  "command": "docker buildx build --platform linux/amd64,linux/arm64 -t <image> --push .",
  "description": "Build and push multi-arch image."
}
