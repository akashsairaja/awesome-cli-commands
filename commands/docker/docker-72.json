{
  "id": "docker-72",
  "categoryId": "docker",
  "subcategoryId": "networks",
  "title": "Run with host network",
  "command": "docker run --network=host <image>",
  "description": "Use host's network stack directly."
}
