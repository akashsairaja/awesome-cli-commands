{
  "id": "docker-84",
  "categoryId": "docker",
  "subcategoryId": "compose",
  "title": "Scale service",
  "command": "docker compose up -d --scale <service>=3",
  "description": "Run multiple instances of a service."
}
