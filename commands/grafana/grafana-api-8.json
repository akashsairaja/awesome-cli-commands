{
  "id": "grafana-api-8",
  "categoryId": "grafana",
  "subcategoryId": "grafana-api",
  "title": "Create annotation",
  "command": "curl -X POST http://localhost:3000/api/annotations -H 'Content-Type: application/json' -H 'Authorization: Bearer TOKEN' -d '{\"text\":\"Deployment v1.2\",\"tags\":[\"deploy\"]}'",
  "description": "Add annotation to all dashboards."
}
