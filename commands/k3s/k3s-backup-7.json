{
  "id": "k3s-backup-7",
  "categoryId": "k3s",
  "subcategoryId": "k3s-backup",
  "title": "Snapshot to S3",
  "command": "k3s etcd-snapshot save --name s3-backup --s3 --s3-bucket my-bucket --s3-region us-east-1 --s3-access-key <key> --s3-secret-key <secret>",
  "description": "Save an etcd snapshot directly to an S3-compatible bucket."
}
