{
  "id": "k3s-install-11",
  "categoryId": "k3s",
  "subcategoryId": "k3s-install",
  "title": "Install with TLS SAN",
  "command": "curl -sfL https://get.k3s.io | sh -s - --tls-san my-load-balancer.example.com",
  "description": "Add additional Subject Alternative Name for the TLS certificate."
}
