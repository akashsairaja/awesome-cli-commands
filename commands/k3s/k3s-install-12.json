{
  "id": "k3s-install-12",
  "categoryId": "k3s",
  "subcategoryId": "k3s-install",
  "title": "Join server to HA cluster",
  "command": "curl -sfL https://get.k3s.io | sh -s - server --server https://first-server:6443 --token <token>",
  "description": "Join additional server node to an existing HA etcd cluster."
}
