{
  "id": "k3s-install-7",
  "categoryId": "k3s",
  "subcategoryId": "k3s-install",
  "title": "Install with INSTALL_K3S_EXEC",
  "command": "curl -sfL https://get.k3s.io | INSTALL_K3S_EXEC=\"--disable servicelb --disable traefik\" sh -",
  "description": "Pass server flags via INSTALL_K3S_EXEC env var."
}
