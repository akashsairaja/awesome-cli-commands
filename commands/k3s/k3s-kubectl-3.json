{
  "id": "k3s-kubectl-3",
  "categoryId": "k3s",
  "subcategoryId": "k3s-kubectl",
  "title": "Copy kubeconfig",
  "command": "sudo cp /etc/rancher/k3s/k3s.yaml ~/.kube/config && sudo chown $USER ~/.kube/config",
  "description": "Copy kubeconfig to user directory."
}
