{
  "id": "kafka-group-3",
  "categoryId": "kafka",
  "subcategoryId": "kafka-groups",
  "title": "Reset offset to earliest",
  "command": "kafka-consumer-groups.sh --group my-group --topic my-topic --reset-offsets --to-earliest --execute --bootstrap-server localhost:9092",
  "description": "Reset group offset to beginning."
}
