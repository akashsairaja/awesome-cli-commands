{
  "id": "k8s-127",
  "categoryId": "kubernetes",
  "subcategoryId": "manifests",
  "title": "Dry run (client)",
  "command": "kubectl apply -f manifest.yaml --dry-run=client",
  "description": "Validate manifest without applying."
}
