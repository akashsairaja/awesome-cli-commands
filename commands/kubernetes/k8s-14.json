{
  "id": "k8s-14",
  "categoryId": "kubernetes",
  "subcategoryId": "pods",
  "title": "Get pods by label",
  "command": "kubectl get pods -l app=myapp",
  "description": "Filter pods by label selector."
}
