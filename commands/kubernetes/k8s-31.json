{
  "id": "k8s-31",
  "categoryId": "kubernetes",
  "subcategoryId": "pods",
  "title": "Run temporary pod",
  "command": "kubectl run tmp --rm -it --image=busybox -- /bin/sh",
  "description": "Run temporary interactive pod that deletes on exit."
}
