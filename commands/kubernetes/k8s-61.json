{
  "id": "k8s-61",
  "categoryId": "kubernetes",
  "subcategoryId": "secrets",
  "title": "Create TLS secret",
  "command": "kubectl create secret tls <name> --cert=cert.crt --key=key.key",
  "description": "Create TLS secret from certificate files."
}
