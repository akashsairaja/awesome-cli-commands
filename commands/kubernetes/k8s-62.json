{
  "id": "k8s-62",
  "categoryId": "kubernetes",
  "subcategoryId": "secrets",
  "title": "Create Docker registry secret",
  "command": "kubectl create secret docker-registry <name> --docker-server=<server> --docker-username=<user> --docker-password=<pass>",
  "description": "Create secret for Docker registry auth."
}
