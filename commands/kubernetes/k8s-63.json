{
  "id": "k8s-63",
  "categoryId": "kubernetes",
  "subcategoryId": "secrets",
  "title": "Decode secret value",
  "command": "kubectl get secret <name> -o jsonpath='{.data.key}' | base64 -d",
  "description": "Decode and display secret value."
}
