{
  "id": "mongo-agg-1",
  "categoryId": "mongodb",
  "subcategoryId": "mongo-aggregate",
  "title": "Basic aggregation",
  "command": "db.orders.aggregate([{ $match: { status: \"completed\" } }, { $group: { _id: \"$customerId\", total: { $sum: \"$amount\" } } }])",
  "description": "Match and group with sum."
}
