{
  "id": "packer-build-11",
  "categoryId": "packer",
  "subcategoryId": "packer-build",
  "title": "On error abort",
  "command": "packer build -on-error=abort .",
  "description": "Abort and clean up on build error."
}
