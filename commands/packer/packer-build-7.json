{
  "id": "packer-build-7",
  "categoryId": "packer",
  "subcategoryId": "packer-build",
  "title": "Force build",
  "command": "packer build -force .",
  "description": "Force build even if artifacts exist."
}
