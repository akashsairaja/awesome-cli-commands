{
  "id": "pg-admin-7",
  "categoryId": "postgresql",
  "subcategoryId": "pg-admin",
  "title": "Table sizes",
  "command": "SELECT relname, pg_size_pretty(pg_total_relation_size(relid)) FROM pg_catalog.pg_statio_user_tables ORDER BY pg_total_relation_size(relid) DESC;",
  "description": "List tables by size."
}
