{
  "id": "pg-performance-1",
  "categoryId": "postgresql",
  "subcategoryId": "pg-performance",
  "title": "Slowest queries",
  "command": "SELECT query, calls, total_exec_time, mean_exec_time, rows FROM pg_stat_statements ORDER BY total_exec_time DESC LIMIT 10;",
  "description": "Find top 10 slowest queries by total execution time. Requires pg_stat_statements extension enabled."
}
