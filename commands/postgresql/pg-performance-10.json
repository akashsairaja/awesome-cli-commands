{
  "id": "pg-performance-10",
  "categoryId": "postgresql",
  "subcategoryId": "pg-performance",
  "title": "Long-running queries",
  "command": "SELECT pid, now() - pg_stat_activity.query_start AS duration, query, state FROM pg_stat_activity WHERE state != 'idle' AND query_start < now() - interval '5 minutes' ORDER BY duration DESC;",
  "description": "Find queries running longer than 5 minutes that may need investigation."
}
