{
  "id": "pg-performance-11",
  "categoryId": "postgresql",
  "subcategoryId": "pg-performance",
  "title": "Unused indexes",
  "command": "SELECT relname, indexrelname, idx_scan, pg_size_pretty(pg_relation_size(indexrelid)) AS index_size FROM pg_stat_user_indexes WHERE idx_scan = 0 ORDER BY pg_relation_size(indexrelid) DESC;",
  "description": "Find indexes that have never been scanned. Candidates for removal to save space and speed up writes."
}
