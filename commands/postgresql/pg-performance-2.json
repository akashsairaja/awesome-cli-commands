{
  "id": "pg-performance-2",
  "categoryId": "postgresql",
  "subcategoryId": "pg-performance",
  "title": "Database statistics",
  "command": "SELECT datname, numbackends, xact_commit, xact_rollback, blks_read, blks_hit, tup_returned, tup_fetched FROM pg_stat_database;",
  "description": "View database-level statistics including commits, rollbacks, block reads, and cache hits."
}
