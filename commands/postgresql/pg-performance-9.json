{
  "id": "pg-performance-9",
  "categoryId": "postgresql",
  "subcategoryId": "pg-performance",
  "title": "Active connections",
  "command": "SELECT state, count(*) FROM pg_stat_activity GROUP BY state;",
  "description": "Count connections by state (active, idle, idle in transaction)."
}
