{
  "id": "pg-query-2",
  "categoryId": "postgresql",
  "subcategoryId": "pg-query",
  "title": "Long running queries",
  "command": "SELECT pid, now() - pg_stat_activity.query_start AS duration, query FROM pg_stat_activity WHERE state = 'active' ORDER BY duration DESC;",
  "description": "Find slow running queries."
}
