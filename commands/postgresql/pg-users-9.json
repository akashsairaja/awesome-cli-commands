{
  "id": "pg-users-9",
  "categoryId": "postgresql",
  "subcategoryId": "pg-users",
  "title": "Grant read-only on all tables",
  "command": "GRANT SELECT ON ALL TABLES IN SCHEMA public TO readonly;",
  "description": "Grant SELECT-only access to all existing tables in the public schema."
}
