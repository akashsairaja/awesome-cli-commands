{
  "id": "prisma-migrate-resolve-rolled-back",
  "categoryId": "prisma",
  "subcategoryId": "prisma-migrate",
  "title": "Mark migration as rolled back",
  "command": "npx prisma migrate resolve --rolled-back <migration>",
  "description": "Mark a failed migration as rolled back to unblock the migration table",
  "tags": ["migrate", "resolve", "rollback"]
}
