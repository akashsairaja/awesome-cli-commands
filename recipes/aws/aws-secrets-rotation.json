{
  "id": "aws-secrets-rotation",
  "stackId": "aws",
  "name": "Manage and Rotate Secrets with AWS Secrets Manager",
  "description": "Store, retrieve, and configure automatic rotation of secrets using AWS Secrets Manager for database credentials, API keys, and other sensitive data.",
  "difficulty": "intermediate",
  "timeMinutes": 12,
  "trending": false,
  "tags": ["secrets-manager", "security", "rotation", "credentials"],
  "prerequisites": [
    "AWS CLI v2 installed and configured",
    "IAM permissions for Secrets Manager operations"
  ],
  "steps": [
    {
      "title": "Create a new secret",
      "description": "Store a new secret value in Secrets Manager with a descriptive name and optional tags for organization.",
      "command": "aws secretsmanager create-secret --name prod/my-app/db-credentials --description \"Production database credentials\" --secret-string '{\"username\":\"admin\",\"password\":\"S3cureP@ss!\",\"host\":\"mydb.cluster-example.us-east-1.rds.amazonaws.com\",\"port\":5432}' --tags Key=Environment,Value=production Key=Application,Value=my-app",
      "warning": "Avoid putting secrets directly in shell history. Use --secret-string file://secret.json to read from a file instead."
    },
    {
      "title": "Retrieve a secret value",
      "description": "Fetch the current secret value and parse specific fields from the JSON payload.",
      "command": "aws secretsmanager get-secret-value --secret-id prod/my-app/db-credentials --query SecretString --output text | jq -r '.password'",
      "tip": "Use --version-stage AWSPREVIOUS to retrieve the previous version of the secret, useful for verifying rotation worked correctly."
    },
    {
      "title": "Enable automatic rotation",
      "description": "Configure a Lambda rotation function to automatically rotate the secret on a schedule.",
      "command": "aws secretsmanager rotate-secret --secret-id prod/my-app/db-credentials --rotation-lambda-arn arn:aws:lambda:us-east-1:111111111111:function:SecretsManagerRDSRotation --rotation-rules AutomaticallyAfterDays=30",
      "tip": "AWS provides pre-built rotation Lambda functions for RDS, Redshift, and DocumentDB. Use the Serverless Application Repository to deploy them."
    },
    {
      "title": "Trigger an immediate rotation",
      "description": "Manually trigger a rotation cycle to test the rotation function or rotate a potentially compromised credential immediately.",
      "command": "aws secretsmanager rotate-secret --secret-id prod/my-app/db-credentials",
      "warning": "Rotation temporarily creates a new version staged as AWSPENDING. Ensure your application can handle brief credential transitions during the rotation window."
    },
    {
      "title": "List secret versions and stages",
      "description": "Inspect the version history and staging labels to understand the current state of the secret and verify rotation completed successfully.",
      "command": "aws secretsmanager list-secret-version-ids --secret-id prod/my-app/db-credentials --query 'Versions[*].{VersionId:VersionId,Stages:VersionStages,Created:CreatedDate}' --output table"
    }
  ],
  "fullScript": "#!/bin/bash\n# Secrets Manager Operations\n# Usage: ./manage-secret.sh <action> <secret-name> [secret-file]\n# Actions: create, get, rotate, list\n\nset -euo pipefail\n\nACTION=\"${1:?Usage: $0 <create|get|rotate|list> <secret-name> [secret-file]}\"\nSECRET_NAME=\"${2:?Provide secret name}\"\n\ncase \"$ACTION\" in\n  create)\n    SECRET_FILE=\"${3:?Provide path to secret JSON file}\"\n    if [ ! -f \"$SECRET_FILE\" ]; then\n      echo \"Secret file not found: $SECRET_FILE\"\n      exit 1\n    fi\n    aws secretsmanager create-secret \\\n      --name \"$SECRET_NAME\" \\\n      --description \"Managed secret for $SECRET_NAME\" \\\n      --secret-string \"file://$SECRET_FILE\"\n    echo \"Secret created: $SECRET_NAME\"\n    rm -f \"$SECRET_FILE\"\n    echo \"Secret file deleted for security.\"\n    ;;\n\n  get)\n    echo \"=== Secret: $SECRET_NAME ===\"\n    aws secretsmanager get-secret-value \\\n      --secret-id \"$SECRET_NAME\" \\\n      --query SecretString --output text | jq .\n    ;;\n\n  rotate)\n    echo \"Triggering rotation for $SECRET_NAME ...\"\n    aws secretsmanager rotate-secret --secret-id \"$SECRET_NAME\"\n    echo \"Rotation initiated. Check versions:\"\n    aws secretsmanager list-secret-version-ids \\\n      --secret-id \"$SECRET_NAME\" \\\n      --query 'Versions[*].{VersionId:VersionId,Stages:VersionStages}' \\\n      --output table\n    ;;\n\n  list)\n    echo \"=== Versions for $SECRET_NAME ===\"\n    aws secretsmanager list-secret-version-ids \\\n      --secret-id \"$SECRET_NAME\" \\\n      --query 'Versions[*].{VersionId:VersionId,Stages:VersionStages,Created:CreatedDate}' \\\n      --output table\n    ;;\n\n  *)\n    echo \"Unknown action: $ACTION\"\n    echo \"Valid actions: create, get, rotate, list\"\n    exit 1\n    ;;\nesac",
  "faq": [
    {
      "question": "What happens if the rotation Lambda function fails?",
      "answer": "If rotation fails, the secret stays at its current version (AWSCURRENT). The AWSPENDING version is abandoned. CloudWatch logs from the rotation Lambda will contain error details. Applications continue using the existing credentials without disruption."
    },
    {
      "question": "How is Secrets Manager different from SSM Parameter Store SecureString?",
      "answer": "Secrets Manager supports automatic rotation via Lambda, cross-account access policies, and per-secret resource policies. Parameter Store SecureString is cheaper ($0 for standard parameters) but has no built-in rotation. Use Secrets Manager for credentials that need rotation; Parameter Store for configuration values."
    },
    {
      "question": "Can I share secrets across AWS accounts?",
      "answer": "Yes. Attach a resource policy to the secret that grants the other account's IAM principal secretsmanager:GetSecretValue permission. The consuming account also needs a KMS key grant if the secret is encrypted with a customer-managed key."
    }
  ],
  "relatedRecipes": ["aws-assume-role-workflow", "aws-rds-snapshot-restore"],
  "lastUpdated": "2026-03-11"
}
