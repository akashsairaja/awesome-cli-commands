{
  "id": "secret-rotation-automation",
  "stackId": "kubernetes",
  "name": "Secret Rotation Automation",
  "description": "Manage and rotate Kubernetes secrets safely, including creating, updating, and verifying secret propagation to pods.",
  "difficulty": "advanced",
  "timeMinutes": 15,
  "trending": false,
  "tags": ["secrets", "security", "rotation", "automation"],
  "prerequisites": ["kubectl configured", "Understanding of Kubernetes secrets"],
  "steps": [
    {
      "title": "List all secrets in a namespace",
      "description": "View existing secrets and their types.",
      "command": "kubectl get secrets -n <namespace> -o custom-columns='NAME:.metadata.name,TYPE:.type,AGE:.metadata.creationTimestamp'"
    },
    {
      "title": "Create a new secret from literal values",
      "description": "Create a secret with key-value pairs from the command line.",
      "command": "kubectl create secret generic db-credentials --from-literal=username=admin --from-literal=password=$(openssl rand -base64 24) --dry-run=client -o yaml | kubectl apply -f -",
      "tip": "Using --dry-run=client -o yaml | kubectl apply allows you to update existing secrets without deleting them first."
    },
    {
      "title": "View a secret's contents (decoded)",
      "description": "Decode and display the values stored in a secret.",
      "command": "kubectl get secret db-credentials -o jsonpath='{.data}' | jq 'to_entries[] | {key: .key, value: (.value | @base64d)}'",
      "warning": "Be careful displaying decoded secrets in shared terminals or CI logs."
    },
    {
      "title": "Restart pods to pick up rotated secrets",
      "description": "After updating a secret, restart the deployment so pods get the new values.",
      "command": "kubectl rollout restart deployment/<deployment-name>",
      "tip": "Alternatively, mount secrets as volumes instead of environment variables. Volume-mounted secrets are updated automatically without pod restarts."
    },
    {
      "title": "Verify pods have the updated secret",
      "description": "Check that running pods see the new secret values.",
      "command": "kubectl exec <pod-name> -- env | grep -i 'DB_\\|SECRET_\\|PASSWORD'"
    }
  ],
  "fullScript": "#!/bin/bash\n# Secret Rotation Workflow\nset -euo pipefail\n\nSECRET_NAME=${1:?\"Usage: $0 <secret-name> [namespace]\"}\nNS=${2:-\"default\"}\n\necho \"=== Current Secret ===\"\nkubectl get secret \"$SECRET_NAME\" -n \"$NS\" -o jsonpath='{.metadata.creationTimestamp}' 2>/dev/null && echo \"\" || echo \"Secret not found\"\n\necho \"\"\necho \"=== Secret Keys ===\"\nkubectl get secret \"$SECRET_NAME\" -n \"$NS\" -o jsonpath='{.data}' 2>/dev/null | jq -r 'keys[]' || echo \"No keys found\"\n\necho \"\"\necho \"=== Pods Using This Secret ===\"\nkubectl get pods -n \"$NS\" -o json | jq -r --arg secret \"$SECRET_NAME\" '.items[] | select(.spec.containers[].env[]?.valueFrom.secretKeyRef.name == $secret or .spec.volumes[]?.secret.secretName == $secret) | .metadata.name' 2>/dev/null || echo \"None found\"\n\necho \"\"\necho \"=== Rotation Steps ===\"\necho \"1. Update:  kubectl create secret generic $SECRET_NAME --from-literal=key=newvalue --dry-run=client -o yaml | kubectl apply -f -\"\necho \"2. Restart: kubectl rollout restart deployment/<name> -n $NS\"\necho \"3. Verify:  kubectl exec <pod> -- env | grep KEY\"",
  "faq": [
    {
      "question": "Do I need to restart pods after rotating a secret?",
      "answer": "If the secret is used as an environment variable, yes. If mounted as a volume, Kubernetes updates the file automatically (with a delay of up to kubelet sync period, typically 1 minute)."
    },
    {
      "question": "How do I encrypt secrets at rest?",
      "answer": "Enable encryption at rest in the kube-apiserver configuration with an EncryptionConfiguration resource. For external secret management, consider tools like External Secrets Operator or Sealed Secrets."
    }
  ],
  "relatedRecipes": ["rbac-auditing-workflow", "namespace-management"],
  "lastUpdated": "2026-03-11"
}
